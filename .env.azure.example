# Address reachable from the frontend container, without a trailing slash.
# Same Docker Compose network: http://backend:8080
# Backend installed directly on this VM: http://172.17.0.1:8080 (or its private VM IP)
# Backend on another VM: http://10.0.0.4:8080
BACKEND_UPSTREAM=http://10.0.0.4:8080
