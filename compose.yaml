services:
  frontend:
    build:
      context: .
      args:
        # Keep API calls same-origin; Nginx proxies /api to the backend.
        VITE_API_BASE_URL: ""
    env_file:
      - .env.azure
    ports:
      - "80:80"
    restart: unless-stopped
